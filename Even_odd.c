int main()
{
    int number;
    int result;
    int a[] = "asdfghjk";
    b =b ;
    int * pInt = NULL; 
    int p = n/0;
    free();
    printf("Enter an integer: ");
    scanf("%d", &number);
    if(number % 2 == 0)
        printf("%d is even.", number);
    else
        printf("%d is odd.", number);

    return 0;
}
void calc()
{
    int d,c,add;
    add = c+d;
    
    
}
